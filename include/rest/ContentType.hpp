#if !defined(CONTENT_TYPE_HPP)
#define CONTENT_TYPE_HPP

enum class ContentType
{
  TEXT_PLAIN,
  TEXT_HTML,
  APPLICATION_JSON,
  APPLICATION_XML,
  IMAGE_JPEG,
  IMAGE_PNG,
  AUDIO_MPEG,
  VIDEO_MP4
};

#endif // CONTENT_TYPE_HPP
